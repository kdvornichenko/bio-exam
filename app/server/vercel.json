{
	"version": 2,
	"installCommand": "corepack enable && cd ../.. && yarn install",
	"buildCommand": "node vercel-build.js",
	"rewrites": [
		{
			"source": "/(.*)",
			"destination": "/api"
		}
	]
}
